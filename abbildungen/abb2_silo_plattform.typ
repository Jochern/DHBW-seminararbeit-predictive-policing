// Abbildung 2: Silo vs. integrierte Plattform (draw.io PNG)
// einspaltig (passt in eine IEEE-Spalte)

#figure(
  image("2.png", width: 100%),
  caption: [Silo-Struktur (links) vs. integrierte Plattform (rechts).
    Eigene Darstellung in Anlehnung an die dreistufige Architekturlogik
    der Fallstudie. Heuristisches Schema, kein Messergebnis.],
) <abb_silo_plattform>
